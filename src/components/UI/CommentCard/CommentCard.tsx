import React from 'react'
import classes from './CommentCard.module.css'

const CommentCard = (props: {id:number}) => {
  return (
    <div className={classes.card}>
      CommentCard
    </div>
  )
}

export default CommentCard